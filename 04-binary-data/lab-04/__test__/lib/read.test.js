'use strict';

const read = require('../../lib/read.js');

describe('Testing the read module', () => {
  it('should read a bmp file when passed a file path as a parameter.', () => {
    expect(read(path, (err, buffer)).toBe())
  })
})